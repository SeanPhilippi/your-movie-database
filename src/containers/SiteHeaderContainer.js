import { connect } from 'react-redux';
import SiteHeader from '../components/SiteHeader';
import { } from '../actions';

function mapStateToProps(state) {
  return {

  }
}

export default connect(mapDispatchToProps)(SiteHeader);